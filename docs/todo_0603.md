# openai_grace_agent 改修 TODO（2026-06-03）

参照資料: `anthropic_grace_agent/docs/benchmark_todo.md`、`anthropic_grace_agent` 改修済みコード

---

## 前提・現状整理

| ファイル | 現状 | 必要な対応 |
|---|---|---|
| `down_load_non_qa_rag_data_from_huggingface.py` | `width='stretch'` (旧Streamlit API) | `use_container_width=True` に修正（3箇所） |
| `chunking/csv_text_to_chunks_text_csv.py` | 概ね OpenAI 対応済み | コメント内サンプルコマンドのモデル名確認のみ |
| `celery_tasks.py` | 差分なし（ヘルパー経由で吸収） | 変更不要 |
| `start_celery.sh` | 両プロジェクト完全一致 | 変更不要 |
| **`run_benchmark_all.sh`** | **ファイル未存在** | **新規作成が必要** |
| `grace/executor.py` | PR #8 で CONFIRM 修正済み | 完了 |
| `helper/helper_rag.py` | 差分なし（anthropic と同一） | 変更不要 |
| `qa_generation/smart_qa_generator.py` | `anthropic` → `openai` 未移植 | API・モデル・パラメータを修正 |

---

## Phase 別 TODO リスト

### Phase 1 — `down_load_non_qa_rag_data_from_huggingface.py`

**作業内容**: Streamlit API の廃止メソッド修正

```diff
# 3箇所を修正（L917, L928, L1059）
- st.dataframe(df.head(10), width='stretch')
+ st.dataframe(df.head(10), use_container_width=True)
```

- `anthropic_grace_agent` では既に修正済み

---

### Phase 2 — `chunking/csv_text_to_chunks_text_csv.py`

**作業内容**: コメント内サンプルコマンドの整合確認

- anthropic版では先頭に `claude-sonnet-4-6` を使うサンプルブロックがあり、openai版は `gpt-5.4-mini` に変更済み
- **主要ロジックの差分はなし** — スクリプト本体は変更不要
- ただし anthropic版に追加された「**固定ファイル名出力**（`--timestamp` なしはタイムスタンプなし）」機能が openai版に未反映の可能性あり → 確認が必要

---

### Phase 3 — `qa_generation/smart_qa_generator.py`

**作業内容**: Anthropic API → OpenAI API への移植

```diff
# L37: デフォルトモデル
- def __init__(self, model: str = "claude-sonnet-4-6", api_key: Optional[str] = None):
+ def __init__(self, model: str = "gpt-4o-mini", api_key: Optional[str] = None):

# L49-50: LLMクライアント初期化
- self.llm = create_llm_client("anthropic", default_model=self.model)
- logger.info(f"Anthropic API を使用 (model={self.model})")
+ self.llm = create_llm_client("openai", default_model=self.model)
+ logger.info(f"OpenAI API を使用 (model={self.model})")

# L72: トークンパラメータ名
- max_tokens=4096,
+ max_completion_tokens=4096,  # gpt-5.4-mini以降は max_completion_tokens

# L397-401: APIキー取得
- api_key = os.getenv("ANTHROPIC_API_KEY")
- print("エラー: ANTHROPIC_API_KEYが設定されていません")
+ api_key = os.getenv("OPENAI_API_KEY")
+ print("エラー: OPENAI_API_KEYが設定されていません")
```

---

### Phase 3 — `run_benchmark_all.sh`（新規作成）

**作業内容**: anthropic版の `run_benchmark.sh` を OpenAI 向けに作成

```bash
#!/usr/bin/env bash
# run_benchmark_all.sh - openai_grace_agent ベンチマーク実行
#
# 前提条件:
#   - Qdrant が起動済み (localhost:6333)
#   - cc_news_1per コレクションが作成・embedding 済み
#   - .env または環境変数に OPENAI_API_KEY が設定済み

set -euo pipefail

COLLECTION="cc_news_1per"
PROJECT="openai_grace_agent"
MODEL="gpt-5.4-mini"

echo "================================================================"
echo "  GRACE Benchmark Runner"
echo "  Project   : ${PROJECT}"
echo "  Model     : ${MODEL}"
echo "  Collection: ${COLLECTION}"
echo "  Start     : $(date '+%Y-%m-%d %H:%M:%S')"
echo "================================================================"

uv run python - << PYEOF
from grace.benchmark import BenchmarkRunner

runner = BenchmarkRunner(qdrant_collection="${COLLECTION}")
sessions = runner.run_query_set(runs_per_query=3)
count = len(sessions)
print(f"\n完了: {count} セッション -> logs/benchmark_results.csv")
PYEOF

echo "================================================================"
echo "  End: $(date '+%Y-%m-%d %H:%M:%S')"
echo "================================================================"
```

---

### Phase 3 — `grace/executor.py`

**状態: PR #8 でマージ済み** — CONFIRM 自動続行修正は完了。追加対応不要。

---

### Phase 3 — `celery_tasks.py` / `start_celery.sh` / `helper/helper_rag.py`

**状態: 変更不要** — 差分なし、または API 差異はヘルパー層で吸収済み。

---

## 実施優先順

```
[必須・即時]
1. qa_generation/smart_qa_generator.py  → anthropic→openai 移植（Q/A生成の中核）
2. run_benchmark_all.sh                 → 新規作成（ベンチマーク実行に必要）

[推奨・軽作業]
3. down_load_non_qa_rag_data_from_huggingface.py → use_container_width 修正（UI表示エラー防止）

[確認のみ]
4. chunking/csv_text_to_chunks_text_csv.py → 固定ファイル名出力仕様の確認
```

---

## 参考：benchmark_todo.md との対応

| benchmark_todo フェーズ | 本 TODO との対応 |
|---|---|
| Phase 1 完了済み（benchmark.py 配備等） | 済み |
| TODO-1: schemas.py フィールド整合チェック | grace/executor.py PR #8 で確認・修正済み |
| TODO-2: temperature=0.0 設定確認 | grace/config.yml で要確認 |
| TODO-3: ローカルインフラ確認 | Qdrant 起動・OPENAI_API_KEY 設定確認 |
| TODO-4〜6: 予備実験 | run_benchmark_all.sh 作成後に実施 |
| TODO-7: 本番 216 セッション | 上記改修完了後に実施 |
| TODO-8〜12: 分析・記事化 | Phase 4（後日） |

---

*作成日: 2026-06-03*
